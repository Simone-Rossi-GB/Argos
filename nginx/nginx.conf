http {
    
    # http
    server {
        listen 80;
    
        include /etc/nginx/locations.conf;
    }
    
    # https decommentare se non si usa ngrok
    # server {
    #    listen 443 ssl;
    #    ssl_certificate /etc/nginx/certs/cert.pem;
    #    ssl_certificate_key /etc/nginx/certs/key.pem;
    #
    #    include /etc/nginx/locations.conf;
    # }
}
